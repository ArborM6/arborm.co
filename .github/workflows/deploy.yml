# name: Deploy

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# env:
#   REGISTRY: crpi-4hl324gk7yawrplw.cn-hangzhou.personal.cr.aliyuncs.com
#   IMAGE_REPOSITORY: arborm-temp/arborm.com
#   IMAGE_TAG: lastest
#   CONTAINER_NAME: arborm.com
#   CONTAINER_PORT: "3000"

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Alibaba Cloud Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
#           password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

#       - name: Build and push image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           push: true
#           tags: |
#             ${{ env.REGISTRY }}/${{ env.IMAGE_REPOSITORY }}:${{ env.IMAGE_TAG }}
#             ${{ env.REGISTRY }}/${{ env.IMAGE_REPOSITORY }}:latest
#             ${{ env.REGISTRY }}/${{ env.IMAGE_REPOSITORY }}:${{ github.sha }}

#       - name: Deploy on server
#         uses: appleboy/ssh-action@v1.2.0
#         env:
#           REGISTRY: ${{ env.REGISTRY }}
#           IMAGE_REPOSITORY: ${{ env.IMAGE_REPOSITORY }}
#           IMAGE_TAG: ${{ env.IMAGE_TAG }}
#           CONTAINER_NAME: ${{ env.CONTAINER_NAME }}
#           CONTAINER_PORT: ${{ env.CONTAINER_PORT }}
#           ALIYUN_REGISTRY_USERNAME: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
#           ALIYUN_REGISTRY_PASSWORD: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USER }}
#           key: ${{ secrets.SERVER_SSH_KEY }}
#           port: ${{ secrets.SERVER_PORT || 22 }}
#           envs: REGISTRY,IMAGE_REPOSITORY,IMAGE_TAG,CONTAINER_NAME,CONTAINER_PORT,ALIYUN_REGISTRY_USERNAME,ALIYUN_REGISTRY_PASSWORD
#           script: |
#             set -eu

#             IMAGE="$REGISTRY/$IMAGE_REPOSITORY:$IMAGE_TAG"

#             echo "$ALIYUN_REGISTRY_PASSWORD" | docker login "$REGISTRY" -u "$ALIYUN_REGISTRY_USERNAME" --password-stdin
#             docker pull "$IMAGE"

#             if docker ps -a --format '{{.Names}}' | grep -Fx "$CONTAINER_NAME" >/dev/null 2>&1; then
#               docker rm -f "$CONTAINER_NAME"
#             fi

#             docker run -d \
#               --name "$CONTAINER_NAME" \
#               --restart always \
#               -p "$CONTAINER_PORT:$CONTAINER_PORT" \
#               "$IMAGE"

#             docker image prune -f
